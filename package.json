{
  "name": "connectgig",
  "version": "1.0.0",
  "description": "AI-powered Gig Worker Platform",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "web:dev": "turbo run dev --filter=web",
    "api:dev": "turbo run dev --filter=api"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "prettier": "^3.0.0",
    "prisma": "^6.15.0",
    "ts-node": "^10.9.2",
    "turbo": "^1.10.0",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.15.0"
  }
}
